// Placeholder — M5 fills this with the preset grid, movement scripts, and
// the trajectory library (record/replay).

import { Panel } from '../common/Panel'

export function PosesView() {
  return (
    <Panel title="Poses">
      <div className="detecting-card">
        <div className="big">POSES</div>
        <div>preset grid · movement scripts · trajectory record/replay</div>
        <div style={{ marginTop: 8, fontSize: 10 }}>coming in M5</div>
      </div>
    </Panel>
  )
}
